{
  "name": "redis-cluster-client-test",
  "version": "1.0.0",
  "description": "This cluster instance uses a self-signed server certificate for TLS and requires auth in client connections & replications.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test-cluster": "node ./src/test-cluster.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ioredis": "^5.3.1"
  }
}
